import { IGameSessionConfig } from "./IGameSessionConfig";
export declare class GameSessionConfig implements IGameSessionConfig {
    private _availableBets;
    private _creditsAmount;
    private _bet;
    constructor();
    get availableBets(): number[];
    set availableBets(value: number[]);
    get creditsAmount(): number;
    set creditsAmount(value: number);
    get bet(): number;
    set bet(value: number);
}
